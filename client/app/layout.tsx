import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Toaster } from "sonner";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Alemeno - Transaction Anomaly Detection",
  description: "Upload financial statement CSV files to detect anomalies and view AI-powered transaction summaries.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased dark`}
    >
      <body className="min-h-full flex flex-col bg-black text-zinc-50 font-sans">
        <main className="flex-grow flex flex-col">
          {children}
        </main>
        <Toaster theme="dark" closeButton richColors position="bottom-right" />
      </body>
    </html>
  );
}

